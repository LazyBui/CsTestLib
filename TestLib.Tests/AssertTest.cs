using System;
using TestLib.Framework;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Assert = TestLib.Framework.Assert;

namespace TestLib.Tests {
	[TestClass]
	public sealed partial class AssertTest {
		// Intentionally a partial class so as to minimally pollute certain views of the Test Explorer
	}
}
